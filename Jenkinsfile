pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Executing Checkout Stage'
            }
        }

        stage('Build') {
            steps {
                echo 'Executing Build Stage'
            }
        }

        stage('Test') {
            steps {
                echo 'Executing Test Stage'
            }
        }
    }
}
